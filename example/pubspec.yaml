name: magento_storefront_example
description: Example app demonstrating Magento Storefront Flutter SDK
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  magento_storefront_flutter:
    path: ../
  cupertino_icons: ^1.0.8
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
